# 狼煙屋データベース 作業記録（2025年7月17日）

## 🔥 プロジェクト情報
- **場所**: `/Users/ootsukaumihei/Development/twinpeach/characters/data-models/`
- **本番URL**: https://remriatv.github.io/twinpeach/characters/data-models/
- **管理画面**: https://remriatv.github.io/twinpeach/characters/data-models/admin.html
- **GitHubリポジトリ**: REMriaTV/twinpeach
- **Supabase URL**: https://roaucowddadmvxgzrvnu.supabase.co

## 📋 本日の作業内容

### 1. ✅ Supabase保存エラー修正（characteristics問題）
**問題**: 
- `Could not find the 'characteristics' column of 'birds' in the schema cache`エラー
- JavaScriptがネスト構造、Supabaseはフラット構造の不一致

**解決**:
- `characteristics: { appearance: ..., voice: ... }` → `appearance: ..., voice: ...`
- すべてのネスト構造をフラット化
- キャッシュバスティング実装（自動タイムスタンプ付与）

### 2. ✅ 削除ボタン追加
- 編集フォームの左側に赤い削除ボタン配置
- 確認ダイアログ付き
- 編集時のみ表示（新規作成時は非表示）

### 3. ✅ ID採番の改善
**変更前**: `bird_1752745198926_6enx`（複雑なタイムスタンプ）
**変更後**: `bird_001`, `bird_002`...`bird_999`（シンプルな連番）
- 自動で次の番号を割り当て
- 削除された番号は再利用
- 999を超える場合は空き番号を探索

### 4. ✅ 表示順序の変更
- 名前順（あいうえお順）→ ID順（001から順番）に変更
- Supabase: `.order('id')`
- ローカル: `sort((a, b) => a.id.localeCompare(b.id))`

### 5. ✅ 関数エクスポート修正
- `addBirdRecording`（存在しない）→ 正しい関数名に修正
- グローバル関数の公開を整理

### 6. ✅ 画像・録音保存機能の実装（完了）
- Supabase Storage対応コード追加済み
- `uploadToStorage`関数実装
- `dataURLtoBlob`変換関数実装
- バケット名定義: `bird-recordings`, `stone-photos`
- セットアップガイド作成: `SUPABASE_STORAGE_SETUP.md`
- saveBird/saveStone関数にStorageアップロード処理追加
- Supabaseスキーマに`recording_urls`/`photo_urls`カラム追加

## 🚨 重要な変更点

### キャッシュバスティング実装
```javascript
// admin.html内
const version = new Date().getTime();
document.write('<script src="admin-manager.js?v=' + version + '"><\/script>');
```

### 開発ルール追加
- `CLAUDE_DEVELOPMENT_RULES.md` - 全プロジェクト共通ルール
- `CLAUDE_QUICK_RULES.md` - クイックリファレンス
- `dev-server.sh` - ローカル開発サーバー起動スクリプト
- `DEPLOYMENT_CHECKLIST.md` - デプロイ前チェックリスト

## 📁 ファイル構成
```
/characters/data-models/
├── admin.html              # 管理画面（編集）
├── admin-manager.js        # 管理画面JS
├── index.html             # 閲覧画面
├── manager.js             # 閲覧画面JS
├── birds-database.js      # 鳥初期データ
├── stones-database.js     # 石初期データ
├── supabase-schema.sql    # DBスキーマ
├── SUPABASE_STORAGE_SETUP.md  # Storage設定ガイド
├── DEPLOYMENT_CHECKLIST.md    # デプロイチェックリスト
└── dev-server.sh          # 開発サーバー起動
```

## 🔄 次回作業予定

### Supabaseダッシュボードでの設定
1. **録音URLsと写真URLsカラムの追加**
   ```sql
   ALTER TABLE birds ADD COLUMN IF NOT EXISTS recording_urls JSONB;
   ALTER TABLE stones ADD COLUMN IF NOT EXISTS photo_urls JSONB;
   ```

2. **Storage有効化**
   - Supabaseダッシュボード → Storage
   - `bird-recordings`バケット作成
   - `stone-photos`バケット作成
   - 各バケットをPublicに設定

3. **RLSポリシー設定**
   - アップロード・ダウンロード権限の設定
   - `SUPABASE_STORAGE_SETUP.md`参照

### 実装済みの機能
- ✅ 録音ファイルのアップロード処理（saveBird関数内）
- ✅ 写真ファイルのアップロード処理（saveStone関数内）
- ✅ dataURL → Blob変換関数
- ✅ Storage URLをデータベースに保存する処理

## ⚠️ 注意事項

### キャッシュ問題対策
1. 必ずキャッシュクリアまたはシークレットモードで確認
2. GitHub Pages反映に5-10分かかる場合あり
3. ローカル開発推奨: `./dev-server.sh`

### データ構造
- **必ずフラット構造**を維持（ネストNG）
- Supabaseスキーマと完全一致が必要
- `supabase-schema.sql`を参照

### ID管理
- 形式: `bird_001`, `stone_001`
- 最大999まで（それ以降は自動でタイムスタンプ）
- 削除時は番号再利用

## 🛠 トラブルシューティング

### `characteristics`エラーが出る場合
1. ブラウザキャッシュを完全クリア
2. Chrome: 設定→プライバシー→閲覧履歴データ削除
3. Safari: 開発メニュー→キャッシュを空にする

### 保存できない場合
1. コンソールでエラー内容確認
2. ネットワークタブで400エラーの詳細確認
3. データ構造がフラットか確認

## 📝 コミット履歴
- `75cf0f3` - 削除ボタン追加とID採番の改善
- `ae7fa4d` - ID自動生成機能追加と関数エクスポート修正
- `1ab1971` - キャッシュバスティング実装とデプロイ改善
- `64f3b55` - Force GitHub Pages deployment update
- `a31865a` - Fix Supabase 400 error: flatten data structure to match schema

## 📊 Storage機能実装状況

### コード側の実装（完了）
1. **uploadToStorage関数** - ファイルをSupabase Storageにアップロード
2. **dataURLtoBlob関数** - dataURLをBlobに変換
3. **saveBird関数** - 録音ファイルをStorageに保存してURLを記録
4. **saveStone関数** - 写真ファイルをStorageに保存してURLを記録
5. **スキーマ更新** - recording_urls/photo_urlsカラム定義追加

### Supabase側の設定（未実装）
- ⚠️ Storageバケットの作成が必要
- ⚠️ RLSポリシーの設定が必要
- ⚠️ テーブルのカラム追加SQLの実行が必要

### 使用方法
1. Supabaseダッシュボードで上記設定を完了
2. 管理画面から鳥/石を編集
3. 録音/写真をアップロード
4. 保存時に自動的にStorageに保存される

---
作成日: 2025-07-17
最終更新: 2025-07-17 14:00