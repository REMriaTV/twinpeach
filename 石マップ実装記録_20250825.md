# 石マップ実装記録 - 2025年8月25日

## 概要
Twin Peachプロジェクトの石マップ機能の実装と管理画面の位置情報フィールド追加作業の完全記録。

## 作業内容

### 1. 石マップ機能の実装（完了）

#### 実装ファイル
- `/Users/ootsukaumihei/Development/twinpeach/stone-map-v6.html`
- `/Users/ootsukaumihei/Development/twinpeach/stone-map-v6-test.js`
- `/Users/ootsukaumihei/Development/twinpeach/supabase-schema.sql`

#### 主な機能
1. **Leaflet.jsによる地図表示**
2. **ぼんやりモード/はっきりモード切り替え**
   - パスワード: `mokumoku`
   - ぼんやりモード: 都道府県単位で色分け表示
   - はっきりモード: 個別の石の位置をマーカーで表示
3. **Supabase連携**
   - stonesテーブルから位置情報付きデータを取得
4. **GeoJSONによる正確な都道府県形状表示**

### 2. データベーススキーマ更新（完了）

#### 追加した位置情報フィールド
```sql
ALTER TABLE stones ADD COLUMN IF NOT EXISTS location_name TEXT;
ALTER TABLE stones ADD COLUMN IF NOT EXISTS prefecture TEXT;
ALTER TABLE stones ADD COLUMN IF NOT EXISTS city TEXT;
ALTER TABLE stones ADD COLUMN IF NOT EXISTS location_tag TEXT CHECK (location_tag IN ('川・河原', '海岸', '山・その他'));
ALTER TABLE stones ADD COLUMN IF NOT EXISTS location_detail TEXT;
ALTER TABLE stones ADD COLUMN IF NOT EXISTS location_notes TEXT;
ALTER TABLE stones ADD COLUMN IF NOT EXISTS address TEXT;
ALTER TABLE stones ADD COLUMN IF NOT EXISTS map_url TEXT;
ALTER TABLE stones ADD COLUMN IF NOT EXISTS lat DECIMAL(10, 7);
ALTER TABLE stones ADD COLUMN IF NOT EXISTS lng DECIMAL(10, 7);
```

### 3. 管理画面の更新

#### 3-1. 旧管理画面（間違えて更新）
- `/Users/ootsukaumihei/Development/twinpeach/characters/data-models/admin.html`
- `/Users/ootsukaumihei/Development/twinpeach/characters/data-models/admin-manager.js`

#### 3-2. 統合データベース管理画面（正しい管理画面）
- **URL**: https://remriatv.github.io/twinpeach/characters/noroshiya-dev/
- **ファイル**:
  - `/Users/ootsukaumihei/Development/twinpeach/characters/noroshiya-dev/index.html`
  - `/Users/ootsukaumihei/Development/twinpeach/characters/noroshiya-dev/noroshiya-dev-v2.js`

#### 実装した機能
1. **位置情報フィールドの追加**
   - 場所名、都道府県、市区町村、タグ
   - 詳細な場所、場所メモ
   - 住所またはGoogleマップURL
   - 緯度・経度

2. **GoogleマップURL自動抽出機能**
   - 対応パターン:
     - 通常の地図: `@34.1234,135.5678,15z`
     - 場所検索: `/place/場所名/@34.1234,135.5678`
     - ストリートビュー: `!3d34.1234!4d135.5678`
   - 短縮URL（maps.app.goo.gl）は非対応

3. **採取ポイント登録・再利用システム**
   - 採取ポイントをローカルストレージに保存
   - ドロップダウンから選択で自動入力
   - 重複チェック機能付き

### 4. 実装済みのデータ構造

#### saveStoneWithImage関数（noroshiya-dev-v2.js）
```javascript
const formData = {
    id: currentStoneId,
    name: document.getElementById('stone-name').value,
    type: document.getElementById('stone-type').value,
    colors: {
        primary: document.getElementById('stone-color-primary').value,
        secondary: document.getElementById('stone-color-secondary').value || null,
        pattern: document.getElementById('stone-pattern').value || null
    },
    hardness: parseFloat(document.getElementById('stone-hardness').value) || null,
    size_range: document.getElementById('stone-size').value,
    texture: document.getElementById('stone-texture').value,
    transparency: document.getElementById('stone-transparency').value,
    special_features: document.getElementById('stone-features').value,
    is_hiuchiishi: document.getElementById('stone-hiuchiishi').value === 'true',
    image_url: currentStoneImage,
    // 位置情報フィールド
    location_name: document.getElementById('stone-location-name').value || null,
    prefecture: document.getElementById('stone-prefecture').value || null,
    city: document.getElementById('stone-city').value || null,
    location_tag: document.getElementById('stone-location-tag').value || null,
    location_detail: document.getElementById('stone-location-detail').value || null,
    location_notes: document.getElementById('stone-location-notes').value || null,
    address: document.getElementById('stone-address').value || null,
    map_url: document.getElementById('stone-address').value || null,
    lat: document.getElementById('stone-lat').value ? parseFloat(document.getElementById('stone-lat').value) : null,
    lng: document.getElementById('stone-lng').value ? parseFloat(document.getElementById('stone-lng').value) : null
};
```

### 5. 削除したフィールド
- 見つかる場所（found_locations）
- レア度（rarity）
- NGキーワード（ng_keywords）

### 6. 変更したフィールド
- "特別な特徴" → "特徴"（special_features）

## 2025年8月25日 18:00 追加実装

### はっきりモード3段階ポップアップ仕様
1. **第1段階（ホバー）**: エリア名 + ひろった石の個数
2. **第2段階（クリック）**: エリア詳細 + 石のサムネイル一覧
3. **第3段階（サムネイルクリック）**: 石の詳細情報

#### 実装詳細
- 場所ごとに石をグループ化（緯度経度を丸めて同じ場所と判定）
- 1エリアに1ピンで表示
- 石の詳細は独立したモーダルで表示

## 現在の状態

### 動作確認済み
1. 石マップページ: https://remriatv.github.io/twinpeach/stone-map-v6.html
2. 管理画面: https://remriatv.github.io/twinpeach/characters/noroshiya-dev/
3. Supabaseとの連携
4. 位置情報の保存と読み込み
5. はっきりモード3段階ポップアップ仕様

### テストデータ
- stone_001: 赤い艶の石（鳥取県、日野川）
- stone_005: シックな石（香川県、津嶋神社）
- stone_007: くさび石（香川県、父母ヶ浜）
- stone_009: 玄人の石（和歌山県、位置情報追加待ち）

## 次回の作業ポイント

1. **短縮URL対応**（必要な場合）
   - サーバーサイド処理が必要

2. **一括登録機能**
   - CSVインポート等

3. **住所から緯度経度の自動取得**
   - Geocoding APIの利用

## 重要な注意点

1. **管理画面のURL**
   - 正しい: https://remriatv.github.io/twinpeach/characters/noroshiya-dev/
   - 間違い: https://remriatv.github.io/twinpeach/characters/data-models/admin.html

2. **使用しているJSファイル**
   - noroshiya-dev-v2.js（noroshiya-dev.jsではない）

3. **Supabase接続情報**
   - URL: https://roaucowddadmvxgzrvnu.supabase.co
   - ANON_KEY: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...（長いので省略）

## コミット履歴
1. 石マップ管理画面に位置情報フィールドを追加
2. 統合データベース管理画面に位置情報フィールドを追加
3. GoogleマップURLから緯度経度を自動抽出する機能を追加
4. 採取ポイント登録・再利用システムを実装

## ファイル構成
```
/Users/ootsukaumihei/Development/twinpeach/
├── stone-map-v6.html          # 石マップ表示ページ
├── stone-map-v6-test.js       # 石マップJavaScript
├── supabase-schema.sql        # データベーススキーマ
└── characters/
    ├── data-models/           # 旧管理画面（使用しない）
    │   ├── admin.html
    │   └── admin-manager.js
    └── noroshiya-dev/         # 統合データベース管理画面（現在使用）
        ├── index.html
        └── noroshiya-dev-v2.js

```

---
最終更新: 2025年8月25日 17:00
作業者: 大塚海平（ootsukaumihei）